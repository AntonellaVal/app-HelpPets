import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RedescontactoPage } from './redescontacto.page';

describe('RedescontactoPage', () => {
  let component: RedescontactoPage;
  let fixture: ComponentFixture<RedescontactoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RedescontactoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
