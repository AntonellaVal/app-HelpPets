import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AnimaleadopcionPage } from './animaleadopcion.page';

describe('AnimaleadopcionPage', () => {
  let component: AnimaleadopcionPage;
  let fixture: ComponentFixture<AnimaleadopcionPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AnimaleadopcionPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  xit('should create', () => {
    expect(component).toBeTruthy();
  });
});